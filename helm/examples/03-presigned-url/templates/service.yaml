apiVersion: v1
kind: Service
metadata:
  name: {{ include "example-03.fullname" . }}
  namespace: example-03
  labels:
    {{- include "example-03.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.backend.port }}
      protocol: TCP
  selector:
    {{- include "example-03.selectorLabels" . | nindent 4 }}
