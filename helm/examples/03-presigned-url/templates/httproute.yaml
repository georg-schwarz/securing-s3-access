apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: {{ include "example-03.fullname" . }}
  namespace: example-03
  labels:
    {{- include "example-03.labels" . | nindent 4 }}
spec:
  parentRefs:
    - name: {{ .Values.gateway.name }}
      namespace: {{ .Values.gateway.namespace }}
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /api/03-presigned-uri/
      backendRefs:
        - name: {{ include "example-03.fullname" . }}
          namespace: example-03
          port: {{ .Values.service.port }}
    - matches:
        - path:
            type: Exact
            value: /api/login
      backendRefs:
        - name: {{ include "example-03.fullname" . }}
          namespace: example-03
          port: {{ .Values.service.port }}
